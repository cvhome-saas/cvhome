import { MapErrorsPipe } from './map-errors.pipe';

describe('MapErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
