spring:
  application:
    name: core-gateway
server:
  port: ${com.asrevo.cvhome.services.core-gateway.port}
info:
  app:
    name: ${spring.application.name}
management:
  observations:
    key-values:
      application: ${spring.application.name}

