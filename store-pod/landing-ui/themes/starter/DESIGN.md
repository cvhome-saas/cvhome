# starter — DESIGN.md (placeholder)

`starter` is intentionally undesigned: it is the reference implementation of the theme contract and the copy
source for `npm run new-theme <name>`. It has no visual world to document.

A real theme gets its DESIGN.md written **at finish** by the impeccable documenter, from the built theme —
see `.agents/skills/project-structure/references/new-landing-ui-template.md`.
