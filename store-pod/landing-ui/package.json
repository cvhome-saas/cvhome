{
  "name": "store-front",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "storefront",
    "libs/*",
    "themes/*"
  ],
  "scripts": {
    "dev": "npm run build:libs && npm run dev --workspace=storefront",
    "start": "npm run start --workspace=storefront",
    "build": "npm run build:libs && npm run build --workspace=storefront",
    "build:libs": "npm run build:libs-types && npm run build:libs-services && npm run build:libs-hooks",
    "build:libs-types": "npm run build --workspace=libs/types",
    "build:libs-services": "npm run build --workspace=libs/services",
    "build:libs-hooks": "npm run build --workspace=libs/hooks",
    "lint": "eslint storefront libs/ui libs/i18n libs/theme themes",
    "typecheck": "npm run typecheck --workspace=storefront",
    "test": "npm run test --workspaces --if-present",
    "new-theme": "node scripts/new-theme.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "~5.9.3",
    "tsx": "^4.19.0"
  }
}
