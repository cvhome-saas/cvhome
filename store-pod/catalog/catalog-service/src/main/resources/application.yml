spring:
  application:
    name: catalog
  config:
    import: "classpath:common-config.yml"
  security:
    oauth2:
      client:
        registration:
          s2s:
            provider: uaa
            client-id: store-pod-507f1f77@service.store-pod.internal
            client-secret: hLwOF59NEOdMzYYrfxUbQEGVK1uTczj7
            authorization-grant-type: client_credentials
            scope: store_pod
  sql:
    init:
      schema-locations: classpath:init-sql/schema.sql
      data-locations: classpath:init-sql/data-common.sql
  jpa:
    properties:
      hibernate:
        default_schema: ${spring.application.name}

reservation:
  expiry:
    minutes: 45
  cleanup:
    interval: 60000

namastack:
  outbox:
    multicaster:
      enabled: true
    polling:
      fixed:
        interval: 2s
      batch-size: 10
    jpa:
      schema-initialization:
        enabled: false
      schema-name: ${spring.application.name}
cvhome:
  metrics:
    outbox:
      # Outbox gauges (cvhome_outbox_records, cvhome_outbox_oldest_pending_seconds) over this service's outbox table.
      enabled: true
      table: catalog.outbox_record
