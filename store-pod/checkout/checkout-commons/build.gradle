plugins {
    alias(libs.plugins.java.library.conventions)
}

dependencies {
    api project(':store-pod:commons:store-commons')
    // The DTO shapes both frontends already read: ReadableCustomer / CustomerAddress and ReadableCountry.
    api project(':store-pod:commons:customer:customer-commons')
    api project(':store-pod:commons:reference:reference-commons')
    // ReadableCartItem extends ReadableMinimalProduct so the storefront keeps reading description/image/variant.
    api project(':store-pod:catalog:catalog-commons')
    compileOnly libs.jakarta.validation.api
    compileOnly libs.jackson.databind
    compileOnly libs.lombok
    annotationProcessor libs.lombok
}
