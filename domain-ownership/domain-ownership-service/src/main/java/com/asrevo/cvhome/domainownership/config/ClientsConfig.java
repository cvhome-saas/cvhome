package com.asrevo.cvhome.domainownership.config;

/*
import com.asrevo.cvhome.domain.service.AcmService;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.reactive.function.client.WebClient;

import static com.asrevo.cvhome.domain.utils.WebClientsUtils.build;

@Configuration
public class ClientsConfig {
    @Bean
    public AcmService acmService(@Qualifier("defaultMicroServiceBuilder") WebClient.Builder builder) {
        return build(builder, "lb://certificate-manager", AcmService.class);
    }

}
*/
