/*
 * @cvhome-saas/ui-kit/i18n — the translation plumbing the controls need, and the copy they read.
 *
 * The loader itself stays in each application: it is the only place that knows where that app's own
 * dictionaries live, and it is where `withKitCopy` is applied.
 */
export * from './lib/calendar';
export * from './lib/dictionaries';
export * from './lib/transloco-testing';
