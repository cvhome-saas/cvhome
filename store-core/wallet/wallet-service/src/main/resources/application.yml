spring:
  application:
    name: wallet
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: wallet
server:
  forward-headers-strategy: FRAMEWORK
  port: ${com.asrevo.cvhome.services.wallet.port}
info:
  app:
    name: ${spring.application.name}
management:
  observations:
    key-values:
      application: ${spring.application.name}